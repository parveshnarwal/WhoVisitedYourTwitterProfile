<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@drawable/find_my_visitors_bgrnd"
    android:layout_height="wrap_content"
    android:layout_width="match_parent">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/vst_hdr"
        android:contentDescription="@string/app_name"
        android:adjustViewBounds="true"
        android:id="@+id/header"/>

    <ListView
        android:id="@+id/list"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:divider="@android:color/transparent"
        android:dividerHeight="10.0sp"
        android:layout_above="@+id/bottomBar_visitors"
        android:layout_below="@+id/header"/>

    <com.roughike.bottombar.BottomBar
        android:id="@+id/bottomBar_visitors"
        android:layout_alignParentBottom="true"
        android:layout_height="60dp"
        android:layout_width="match_parent"
        app:bb_activeTabColor="#2ba7ef"
        app:bb_inActiveTabColor="#2ba7ef"
        app:bb_tabXmlResource="@xml/bottombar_visitor" />


</RelativeLayout>


